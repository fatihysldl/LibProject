@using EntityLayer.concrete
@model List<borrowedBook>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<table class="table table-bordered">
    <tr>
        <th>Id</th>
        <th>Member Name</th>
        <th>Book Name</th>
        <th>Category Name</th>
        <th>Edit</th>
    </tr>

    <tbody>
        @foreach (var i in Model)
        {
            <tr>
                <td>@i.Id</td>
                <td>@i.member.memberName </td>
                <td>@i.book.bookName </td>
                <td>@i.category.category</td>
                <td>
                    <a href="#" style=" display: flex; align-items: center" class="btn btn-warning">
                        Delete
                    </a>
                    <a href="#" class="btn btn-success" style=" display: flex; align-items: center; margin-top:5px;">
                        Update
                    </a>
                </td>
            </tr>
        }
    </tbody>
</table>

